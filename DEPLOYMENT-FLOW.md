# 🔄 Deployment Flow - Visual Guide

This document explains how the automated deployment works step-by-step.

---

## 🎯 Overview

```
┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│   Your PC    │  Push   │    GitHub    │ Deploy  │  AWS EC2     │
│              ├────────>│   Actions    ├────────>│   Server     │
│   (Local)    │         │  (Workflow)  │         │  (Ubuntu)    │
└──────────────┘         └──────────────┘         └──────────────┘
```

---

## 📊 Complete Deployment Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     1. DEVELOPER ACTION                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Developer makes code changes locally                           │
│  ↓                                                              │
│  git add .                                                      │
│  git commit -m "Update feature"                                │
│  git push origin main                                           │
│                                                                 │
└────────────────────────┬────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                     2. GITHUB DETECTS PUSH                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  GitHub receives push to 'main' branch                          │
│  ↓                                                              │
│  Triggers: .github/workflows/deploy.yml                         │
│  ↓                                                              │
│  Creates Ubuntu runner (virtual machine)                        │
│                                                                 │
└────────────────────────┬────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                   3. GITHUB ACTIONS RUNNER                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Step 1: Checkout Code                                          │
│    ✓ Downloads latest code from repository                     │
│                                                                 │
│  Step 2: Setup SSH Key                                          │
│    ✓ Loads AWS_SSH_PRIVATE_KEY from secrets                    │
│    ✓ Configures SSH agent                                      │
│                                                                 │
│  Step 3: Add EC2 to Known Hosts                                │
│    ✓ Prevents "unknown host" warnings                          │
│                                                                 │
└────────────────────────┬────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                   4. SSH CONNECTION TO EC2                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  GitHub Runner → SSH → AWS EC2 Server                           │
│                                                                 │
│  Connection established using:                                  │
│    • AWS_SSH_PRIVATE_KEY (authentication)                       │
│    • AWS_HOST (destination IP)                                  │
│    • AWS_USER (username: ubuntu)                                │
│                                                                 │
└────────────────────────┬────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│              5. CHECK & INSTALL DEPENDENCIES                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Is Docker installed?                                           │
│    NO → Install Docker                                          │
│    YES → Skip                                                   │
│                                                                 │
│  Is Docker Compose installed?                                   │
│    NO → Install Docker Compose                                  │
│    YES → Skip                                                   │
│                                                                 │
└────────────────────────┬────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                   6. UPDATE APPLICATION CODE                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Does /home/ubuntu/ai-intelligence-hub exist?                   │
│                                                                 │
│  YES:                              NO:                          │
│    • Navigate to directory         • Create directory           │
│    • git pull latest changes       • git clone repository       │
│                                                                 │
└────────────────────────┬────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                7. CONFIGURE ENVIRONMENT                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Does server/.env exist?                                        │
│    NO → Create default .env file                                │
│    YES → Keep existing (preserves your API keys)                │
│                                                                 │
└────────────────────────┬────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                    8. DEPLOY WITH DOCKER                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Step 1: Stop existing containers                               │
│    • docker-compose down                                        │
│                                                                 │
│  Step 2: Build new images                                       │
│    • docker-compose build                                       │
│    • Rebuilds: server, client, mongodb, n8n                     │
│                                                                 │
│  Step 3: Start containers                                       │
│    • docker-compose up -d                                       │
│    • Starts all services in background                          │
│                                                                 │
│  Step 4: Health check                                           │
│    • Wait 10 seconds                                            │
│    • Check container status                                     │
│    • Display logs                                               │
│                                                                 │
└────────────────────────┬────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                      9. DEPLOYMENT COMPLETE                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ✅ Application is now LIVE!                                    │
│                                                                 │
│  Accessible at:                                                 │
│    • Frontend:  http://YOUR_EC2_IP                              │
│    • Backend:   http://YOUR_EC2_IP:5000                         │
│    • n8n:       http://YOUR_EC2_IP:5678                         │
│                                                                 │
│  GitHub Actions marks workflow as SUCCESS ✓                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Secret Flow

```
┌───────────────────────────────────────────────────────────────┐
│                    GitHub Secrets Storage                     │
│  (Encrypted at rest, only accessible to workflows)           │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  🔑 AWS_SSH_PRIVATE_KEY  →  Used for SSH authentication       │
│  🌐 AWS_HOST             →  Your EC2 IP address               │
│  👤 AWS_USER             →  SSH username (ubuntu)             │
│                                                               │
└────────────────────┬──────────────────────────────────────────┘
                     ↓
         ┌───────────────────────┐
         │  GitHub Actions       │
         │  (Reads secrets)      │
         └───────────┬───────────┘
                     ↓
         ┌───────────────────────┐
         │  SSH Connection       │
         │  Established to EC2   │
         └───────────────────────┘
```

**🔒 Security Note**: Secrets are never exposed in logs or workflow output.

---

## 🐳 Docker Container Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                       Docker Compose                            │
│                    (Orchestrates 4 services)                    │
└───────┬─────────────────┬──────────────┬──────────────┬─────────┘
        ↓                 ↓              ↓              ↓
   ┌─────────┐      ┌─────────┐   ┌──────────┐   ┌─────────┐
   │ MongoDB │      │ Server  │   │  Client  │   │   n8n   │
   │ (DB)    │◄─────┤ (API)   │◄──┤ (React)  │   │ (Auto)  │
   │ :27017  │      │ :5000   │   │  :80     │   │ :5678   │
   └─────────┘      └─────────┘   └──────────┘   └─────────┘
        ↑                ↑              ↑              ↑
        │                │              │              │
   Persistent      Node.js with     Nginx serving  Workflow
   Data Storage    Express & AI     React App      Automation
```

### Container Dependencies

```
1. mongodb         (Starts first - no dependencies)
   ↓
2. server          (Waits for mongodb to be healthy)
   ↓
3. client          (Waits for server to be healthy)
   ↓
4. n8n             (Starts independently)
```

---

## ⚡ Build Process Timeline

```
Typical deployment timeline:

00:00  →  GitHub detects push
00:05  →  Runner starts, checks out code
00:10  →  SSH connection established
00:15  →  Checking Docker installation
00:20  →  Pulling latest code
00:25  →  Stopping old containers
00:30  →  Building new images
          ├─ Server build   (30s)
          ├─ Client build   (60s)
          └─ Other images   (10s)
02:00  →  Starting containers
02:10  →  Health checks
02:15  →  ✅ Deployment complete!

Total: ~2-3 minutes
```

---

## 🔄 Rollback Strategy

If deployment fails:

```
┌──────────────────────────────────────────────────────────┐
│                 Automatic Rollback                       │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  Deployment Failed? (Build error, container crash)      │
│    ↓                                                     │
│  Old containers keep running                            │
│    ↓                                                     │
│  No downtime for users                                  │
│    ↓                                                     │
│  Fix issue locally                                      │
│    ↓                                                     │
│  Push again                                             │
│    ↓                                                     │
│  New deployment attempts                                │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

**Manual Rollback** (if needed):

```bash
# SSH into EC2
ssh -i key.pem ubuntu@YOUR_EC2_IP

# Navigate to project
cd ~/ai-intelligence-hub

# View commit history
git log --oneline

# Rollback to previous commit
git reset --hard COMMIT_HASH

# Rebuild
sudo docker-compose up -d --build
```

---

## 📊 Network Flow

```
┌────────────────────────────────────────────────────────────┐
│                        Internet                            │
└────────────┬───────────────────────────────────────────────┘
             ↓
┌────────────────────────────────────────────────────────────┐
│                 AWS Security Group                         │
│  (Firewall Rules)                                          │
│    • Port 22  → SSH (GitHub Actions)                       │
│    • Port 80  → Frontend (Users)                           │
│    • Port 5000 → Backend API (Users)                       │
│    • Port 5678 → n8n (Admin only)                          │
└────────────┬───────────────────────────────────────────────┘
             ↓
┌────────────────────────────────────────────────────────────┐
│                    EC2 Instance                            │
│                    Ubuntu Server                           │
│                                                            │
│  ┌──────────────────────────────────────────────┐         │
│  │           Docker Network                     │         │
│  │                                              │         │
│  │  ┌─────────┐  ┌─────────┐  ┌──────────┐    │         │
│  │  │ MongoDB │  │ Server  │  │  Client  │    │         │
│  │  │         │◄─┤         │◄─┤          │    │         │
│  │  └─────────┘  └─────────┘  └──────────┘    │         │
│  │                                              │         │
│  │  ┌─────────┐                                │         │
│  │  │   n8n   │                                │         │
│  │  └─────────┘                                │         │
│  │                                              │         │
│  └──────────────────────────────────────────────┘         │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 🎯 Success Indicators

### In GitHub Actions:

```
✓ Checkout Code
✓ Setup SSH Key
✓ Add EC2 to Known Hosts
✓ Deploy to EC2
  ✓ Checking Docker installation
  ✓ Docker is already installed
  ✓ Pulling latest code
  ✓ Building and starting containers
  ✓ Container Status: All running
✓ Deployment Status
```

### In Browser:

```
http://YOUR_EC2_IP          → ✅ Dashboard loads
http://YOUR_EC2_IP:5000     → ✅ API responds
http://YOUR_EC2_IP:5678     → ✅ n8n interface
```

### On EC2 Server:

```bash
$ sudo docker-compose ps
NAME             STATUS              PORTS
ai-hub-mongodb   Up (healthy)        27017/tcp
ai-hub-server    Up (healthy)        0.0.0.0:5000->5000/tcp
ai-hub-client    Up                  0.0.0.0:80->80/tcp
ai-hub-n8n       Up                  0.0.0.0:5678->5678/tcp
```

---

## 🚨 Monitoring Deployment

### Real-time Monitoring:

```
GitHub Actions Tab → Watch logs in real-time
     ↓
Each step shows progress
     ↓
Green checkmarks = Success
Red X marks = Failure
```

### Post-Deployment Health Check:

```bash
# Quick health check
curl http://YOUR_EC2_IP:5000/health

# Expected response:
{
  "status": "ok",
  "message": "AI Intelligence Hub API is running",
  "database": "connected",
  "timestamp": "2026-01-23T...",
  "uptime": 123.45
}
```

---

## 💡 Pro Tips

1. **Watch First Deployment**: The first deployment takes longer (Docker installation)
2. **Subsequent Deployments**: Only 2-3 minutes after first deployment
3. **Test Locally First**: Always test changes locally before pushing
4. **Monitor Logs**: Keep GitHub Actions tab open during deployment
5. **SSH Access**: Keep your SSH key handy for manual interventions

---

## 📚 Quick Reference

| Action | Command |
|--------|---------|
| Deploy | `git push origin main` |
| Manual Trigger | GitHub → Actions → Run workflow |
| Check Status | GitHub → Actions tab |
| View Logs | SSH to EC2 → `docker-compose logs` |
| Restart | SSH to EC2 → `docker-compose restart` |
| Rollback | SSH to EC2 → `git reset --hard HASH` |

---

**🎉 Your deployment pipeline is now fully automated!**

Every push to `main` = Automatic deployment to AWS! 🚀
